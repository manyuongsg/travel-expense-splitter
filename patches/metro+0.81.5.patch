diff --git a/node_modules/metro/src/node-haste/DependencyGraph.js b/node_modules/metro/src/node-haste/DependencyGraph.js
index b09fa1e..648527f 100644
--- a/node_modules/metro/src/node-haste/DependencyGraph.js
+++ b/node_modules/metro/src/node-haste/DependencyGraph.js
@@ -35,6 +35,7 @@ class DependencyGraph extends EventEmitter {
   constructor(config, options) {
     super();
     this._config = config;
+    this._resolutionCache = new Map();
     const { hasReducedPerformance, watch } = options ?? {};
     const initializingMetroLogEntry = log(
       createActionStartEntry("Initializing Metro")
@@ -245,6 +246,9 @@ class DependencyGraph extends EventEmitter {
     let resolution = mapByPlatform.get(platformKey);
     if (!resolution) {
       try {
+        if (!this._moduleResolver && this._fileSystem != null && this._moduleCache != null) {
+          this._createModuleResolver();
+        }
         resolution = this._moduleResolver.resolveDependency(
           this._moduleCache.getModule(from),
           dependency,
